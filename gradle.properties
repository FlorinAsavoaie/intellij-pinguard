# The plugin's name and repository URL are not here: they are declared once each,
# in src/main/resources/META-INF/plugin.xml and in GitHubIssueUrl.kt, by the code
# that actually ships them. A copy in this file would only be a third value free
# to drift from the two real ones.
#
# The version is not here either, and for the same reason: the release tag says what
# a release is, and a copy in this file would be free to disagree with it. See
# build.gradle.kts for how a build is told which version it is producing.
pluginGroup = tech.florin.pinguard

# 253 is where JetBrains documents split-mode plugin development. The content
# module this plugin ships loads on older builds too, but nothing there is
# supported, and `runIdeSplitMode` — the only way to actually exercise a
# frontend module — needs 2025.3.
pluginSinceBuild = 253

# No platformType any more: IDEA is published as one distribution from 2025.3,
# so build.gradle.kts asks for `intellijIdea` directly.
platformVersion = 2025.3.6.1

kotlin.stdlib.default.dependency = false

org.gradle.jvmargs = -Xmx3g -XX:MaxMetaspaceSize=1g
org.gradle.caching = true
org.gradle.parallel = true
