<idea-plugin url="https://github.com/FlorinAsavoaie/intellij-pinguard">
    <id>tech.florin.pinguard</id>
    <name>PinGuard</name>
    <vendor email="florin.asavoaie@easyhacks.net" url="https://florin.tech">
        Florin Asăvoaie
    </vendor>

    <description><![CDATA[
    <p>Keeps pinned editor tabs pinned.</p>

    <p>Pinning a tab says "I want to keep this open". The tab's own close button
    respects that, but <em>Close</em> (Cmd+W / Ctrl+F4), <em>Close All</em>,
    <em>Close Others</em> and the rest of the tab context menu will still close a
    pinned tab. PinGuard takes pinned tabs out of those commands.</p>

    <p>Prefer a safety net over a hard stop? Turn on
    <b>Settings | Editor | PinGuard | Ask for confirmation instead of blocking</b>
    and you will be asked before a pinned tab closes, once per close rather than
    once per tab.</p>

    <p>A protected <em>Close All</em> still closes everything that is not pinned —
    keeping a pin never strands the tabs you did mean to close. Unpinned tabs are
    never affected, and closing a project, or the IDE is never interrupted.</p>

    <p>Built for remote development and Code With Me: the guard ships as a
    frontend module so it loads in the JetBrains Client, where the editor tabs
    and the close commands actually live, rather than only on the backend. This
    layout has not yet been confirmed against a live remote session — if you use
    PinGuard that way, reports are welcome.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <!--
      This descriptor deliberately registers nothing. Every extension PinGuard has
      is declared in the module below, because a close action can only be guarded
      in the process that dispatches it — a monolithic IDE, or the JetBrains
      Client in a remote development session, never the backend the plugin is
      installed on.

      The default `optional` loading, and not `required`. `required` reads like the
      stricter, safer choice — a plugin installed, enabled and doing nothing is the
      failure this layout exists to rule out, so why not fail loudly instead? Because
      on a remote development backend, installed and doing nothing *is* the correct
      state: that half of the split owns no editor tabs, which is the entire reason
      the module asks for the frontend. `required` cannot tell "wrong IDE, this can
      never load" from "right setup, wrong half of the pair", so it fails the case
      the plugin exists to serve. Under it, `runIdeSplitMode` logs

          Plugin 'PinGuard' (tech.florin.pinguard) has dependency on
          'intellij.platform.frontend' which is not installed

      and the backend refuses PinGuard outright — while the platform disables its own
      `intellij.platform.monolith` on the same line above it, without complaint.

      The silent-skip this gives up on cannot happen in a monolithic IDE, where
      `intellij.platform.frontend` always resolves. What it protects against there is
      a broken module jar name, and `PluginDescriptorTest` already asserts that.

      Every bundled JetBrains plugin with a frontend module is packaged this way;
      `intellij.editorconfig.frontend` is the one to compare against.
    -->
    <content>
        <module name="tech.florin.pinguard.frontend"/>
    </content>
</idea-plugin>
